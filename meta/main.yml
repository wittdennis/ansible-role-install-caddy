---
galaxy_info:
  author: Dennis Witt
  description: Role to install and manage a Caddy webserver.
  namespace: wittdennis
  role_name: install_caddy
  license: MIT
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Fedora
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: Rocky
      versions:
        - all

  min_ansible_version: "2.1"

  # If this a Container Enabled role, provide the minimum Ansible Container version.
  # min_ansible_container_version:

  galaxy_tags:
    - caddyserver
    - webserver
    - caddy

dependencies:
  []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
